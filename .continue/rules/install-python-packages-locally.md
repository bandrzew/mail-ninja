---
description: Ensure all Python packages are installed locally in the virtual
  environment, not globally.
alwaysApply: true
---

Activate the local virtual environment using 'source ./venv/bin/activate' before running pip install commands.